<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
             https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <!-- Nombre de la unidad de persistencia -->
    <persistence-unit name="oneToManyBi" transaction-type="RESOURCE_LOCAL">

        <!-- 👇 Declarás tus clases JPA aquí -->
        <class>org.example.Persona</class>
        <class>org.example.Domicilio</class>


        <!-- 👇 Propiedades de conexión y configuración de JPA/Hibernate -->
        <properties>

            <!-- Configuración de conexión JDBC -->
            <property name="jakarta.persistence.jdbc.driver" value="org.h2.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:h2:file:./data/oneToManyBi;DB_CLOSE_DELAY=-1;AUTO_SERVER=TRUE"/>
            <property name="jakarta.persistence.jdbc.user" value="sa"/>
            <property name="jakarta.persistence.jdbc.password" value=""/>

            <!-- Dialecto de Hibernate -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.H2Dialect"/>

            <!-- Estrategia de generación del schema -->
            <!-- 'create', 'drop-and-create', 'update', 'validate', 'none' -->
            <property name="jakarta.persistence.schema-generation.database.action" value="drop-and-create"/>

            <!-- Mostrar las sentencias SQL en consola -->
            <property name="hibernate.show_sql" value="true"/>

            <!-- Formatear el SQL en consola -->
            <property name="hibernate.format_sql" value="true"/>

        </properties>
    </persistence-unit>
</persistence>
